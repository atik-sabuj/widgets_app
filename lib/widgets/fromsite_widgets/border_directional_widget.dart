import 'package:flutter/material.dart';

class BorderDirectionalWidget extends StatelessWidget {
  const BorderDirectionalWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
